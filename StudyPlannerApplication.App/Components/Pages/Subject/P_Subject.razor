@page "/subject"
@using StudyPlannerApplication.App.Components.Layout
@using StudyPlannerApplication.Domain.Features.Subject
@layout MainLayout
@inject SubjectService _subjectService
@rendermode InteractiveServer
@if (_formType == EnumFormType.List)
{
    <div class="card">
        <div class="card-header">
            <h5 class="mb-0">
                <button class="btn btn-success btn-sm mb-0" type="button" @onclick=Create>Create</button>
                Subject
            </h5>
        </div>
        @*  <div class="row col-12">
    <div class="col-sm-3 ml-6" style="margin-right: -8px;">
    <RadzenText TextStyle="TextStyle.Subtitle2" TagName="TagName.H3">Image Name</RadzenText>
    <RadzenTextBox @bind-Value=@_reqModel Class="w-100"/>
    </div>
    <div class="col-sm-3 mt-4 mb-6">
    <RadzenButton Click="() => Search()" Text="Search" ButtonStyle="ButtonStyle.Info"/>
    </div>
    </div> *@
        @* @if (lst != null && lst.Count() > 0)
    {
    <div>
    <MudSimpleTable Style="overflow-x: auto;">
    <thead>
    <tr>
    <th>Sr No.</th>
    <th>Image Name</th>
    <th>Image Redirect Link</th>
    <th>Order No</th>
    <th>Action</th>
    </tr>
    </thead>
    <tbody>
    @{
    int count = (ps.PageNo - 1) * ps.PageSize;
    foreach (var item in lst)
    {
    <tr>
    <td>@(++count)</td>
    <td>@item.ImageName</td>
    <td>
    <MudChip T="string" Color="Color.Transparent">
    <a href="@item.ImageRedirectLink" target="_blank">@item.ImageName</a>
    </MudChip>
    </td>
    <td>@item.OrderNo</td>
    <td>
    @{
    if (_permissionMenuModel.IsEditEnable)
    {
    <RadzenButton Icon="edit" ButtonStyle="ButtonStyle.Primary" Click="(args) => Edit(item.AdsPhotoId)"/>
    }
    if (_permissionMenuModel.IsDetailEnable)
    {
    <RadzenButton Icon="pageview" ButtonStyle="ButtonStyle.Info" Click="(args) => Detail(item.AdsPhotoId)"/>
    }
    if (_permissionMenuModel.IsDeleteEnable)
    {
    <RadzenButton Icon="delete" ButtonStyle="ButtonStyle.Secondary" Click="(args) => Delete(item.AdsPhotoId)"/>
    }
    }
    </td>
    </tr>
    }
    }

    </tbody>
    </MudSimpleTable>
    <div class="d-flex flex-column align-center mb-4">
    <MudPagination BoundaryCount="2" MiddleCount="5" SelectedChanged="PageChanged" Count="@count" Class="mt-4"/>
    </div>
    </div>
    }
    else
    {
    <div class="col-sm-3 ml-6" style="margin-right: -8px;">
    <p>No Record To Show</p>
    </div>
    } *@
    </div>
}
@if (_formType == EnumFormType.Register)
{
    <div class="card">
        <div class="card-body">
            <h5 class="font-weight-bolder">@(_formType.ToString()) Subject</h5>
            <div class="row">
                <div class="mb-3">
                    <label for="exampleFormControlInput1" class="form-label">Subject Name</label>
                    <input type="email" class="form-control" @bind-value="_reqModel.SubjectName" >
                </div>
                <div class="mb-3">
                    <label for="exampleFormControlTextarea1" class="form-label">Description</label>
                    <textarea class="form-control" @bind="_reqModel.Description"></textarea>
                </div>
                <div class="col-12 mt-4">
                    <div class="d-flex">
                        <button @onclick=Back class="btn btn-outline-dark btn-sm mb-0 me-2" type="button">Back</button>
                        <button class="btn btn-success btn-sm mb-0" type="button" @onclick=Save>@_formType.ToString()</button>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
